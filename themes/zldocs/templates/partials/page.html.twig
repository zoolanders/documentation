<div id="body-inner">
	<h1>{{ page.title }}</h1>

	{% block content %}
		<p>
		    {{ page.content }}
		</p>
	    {% for module in page.collection() %}
	        {{ module.content }}
	    {% endfor %}
	{% endblock %}
</div>

{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "lint": "next lint",
    "http": "npx http-server",
    "sitemap": "node ./scripts/sitemap.mjs",
    "checklinks": "npx linkinator http://localhost:3000 -r --verbosity error"
  },
  "devDependencies": {
    "lodash-es": "^4.17.21",
    "prettier": "^3.2.5",
    "vitepress": "^1.0.1",
    "vue": "^3.4.21"
  }
}

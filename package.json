{
  "private": true,
  "version": "2.1.0",
  "scripts": {
    "dev": "vuepress dev src",
    "build": "vuepress build src",
    "watch": "npm run dev",
    "checklinks": "npx linkinator ./dist --recursive --skip zoolanders.com"
  },
  "devDependencies": {
    "vuepress": "^1.0.0"
  }
}
